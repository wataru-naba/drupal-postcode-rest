{% if title %}
  <h3>{{ title }}</h3>
{% endif %}

<div class="bbs-list">
  {% for row in rows %}
    {{ row.content }}
  {% else %}
    <p class="bbs-list__empty">{{ '投稿はまだありません。'|t }}</p>
  {% endfor %}
</div>
